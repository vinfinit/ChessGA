#ifndef CHESSPIECE_H
#define CHESSPIECE_H

#include <vector>

typedef std::vector<std::vector<int>> MoveList;
typedef std::vector<int> Move;
typedef std::vector<int>::size_type MoveType;

class ChessPiece
{
private:
    Move curPos;
public:
    ChessPiece(Move curPos);
    ~ChessPiece();
    
    virtual MoveList availableStep() = 0;
};

#endif // CHESSPIECE_H
